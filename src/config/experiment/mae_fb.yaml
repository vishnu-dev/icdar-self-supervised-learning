# @package _global_
defaults:
  - /model/vision_transformers/tiny@model.params
  - override /paths: default
  - override /dataset: icdar
  - override /model: mae
  - override /mode: train
  - override /trainer: default

paths:
  icdar_root_dir: /home/woody/iwfa/iwfa028h/dev/faps/data/ICDAR2017_CLaMM_Large
  icdar_label_path: /home/woody/iwfa/iwfa028h/dev/faps/data/ICDAR2017_CLaMM_Large/@ICDAR2017_CLaMM_Large.csv

trainer:
  strategy:
  max_epochs: 100
  train_val_test_split: [0.7, 0.1, 0.2]
  accumulate_grad_batches: 8
  
dataset:
  batch_size: 32

model:
  name: mae
  image_size: 384
  learning_rate: 0.01
